package day60_polymorphism2;

public class InterviewQuestions {
/*IQ: If you created an object polymorphic way, 
 * how do you find out actual object type?
 * 
 * ANimal an = new Crocodile();
 * we can use getClass().getName();
 *            getClass().getSimpleName();
 *            
 *            an.getClass().getName() ==>
 *            
 *    ANy other way?
 *    instanceof operator can be used to check if the object is certain class.
 *    
 *    Animal an = new Koala();
 *    
 *    if(an instanceof Koala){
 *    
 *    
 *   
 *    
 *    2 important Rules of polymorphism
 *    1)Reference type can be parent class or interface and object can be any child class
 *    2)Reference type decides what is accessible/ what can be used.
 * 
 * 
 */
}
